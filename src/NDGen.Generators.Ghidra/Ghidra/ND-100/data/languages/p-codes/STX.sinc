:STX disp is op8=0x18 & disp {  # 014000 octal = 0x1800, top 8 bits = 0x18
    local addr:2 = inst_start + 1 + disp;   # P-relative addressing (word-addressed)
    *:2 addr = X;                           # Store X to memory location
}